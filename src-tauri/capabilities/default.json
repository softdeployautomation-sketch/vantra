{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability set for the Vantra desktop shell (thin local license-gate runtime).",
  "windows": ["*"],
  "permissions": ["core:default"]
}